/** @deprecated Use @/lib/animation/deriveActions.js for dev debug. File kept to avoid broken imports. */
export {
  describeBeatActions as deriveBeatActions,
  formatBeatLine as formatBeatDerivation,
  formatAllBeatLines as formatAllDerivations,
  debugPlayerRowsFromActions as debugPlayerRows,
  PLAYER_IDS,
} from "@/lib/animation/deriveActions";

export const MOVE_MS = 2400;
export const HOLD_MS = 900;
