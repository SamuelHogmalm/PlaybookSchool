# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Spec authority

`MASTER-BUILD-PLAN.md` in the repo root is the authoritative spec. Read it before writing
code. Where any other document, comment, or prior instruction conflicts with it, it wins.
It defines the data model, the twelve validation rules, the timing lanes, and the phased
build order — all of which are implemented literally in the code.

`AGENTS.md` carries the same pointer plus a Next.js block auto-regenerated by `next dev`.

`DECISIONS.md` records *why* significant choices were made, with dates. It is not
authoritative — the plan is — but read it before re-opening a settled question or
undoing something that looks arbitrary. Add an entry when a choice closes off an
alternative someone might reasonably try again later.

`PROPOSALS.md` holds work deliberately *not* done, with the reasoning to decide against
it quickly. `OVERNIGHT.md` is the log of the 13 Aug session.

## Commands

```bash
npm run dev                  # next dev
npm run build                # next build
npm run lint                 # eslint (flat config; _legacy/ ignored)
npm test                     # tsx --test over tests/play/**/*.test.ts
npx tsx --test tests/play/timing.test.ts          # single test file
npx tsx --test --test-name-pattern "ball" tests/play/timing.test.ts

npm run test:seed            # validatePlay() over every play in plays-interpreted.json
npm run build:repairs        # regenerate src/data/repairs-review.json for /dev/repairs
npm run seed:test-users      # needs SUPABASE_SERVICE_ROLE_KEY in .env.local
```

Importer service (separate Python process, not on Vercel):

```bash
cd services/importer && .venv/Scripts/activate && uvicorn main:app --reload --port 8000
```

Data-pipeline scripts under `scripts/` are run directly (`python scripts/re-derive-seed.py`,
`npx tsx scripts/find-teleport-gaps.ts`). Several hardcode a local PDF path — check the
constants at the top before running.

## Architecture

One play format. Three producers (builder, PDF importer, seed data), one animator, three
consumers (builder preview, review flow, quiz engine). The importer's job is to produce
exactly what the builder produces; the quiz engine never knows where a play came from.

Singletons that must never be duplicated — if positions or validity are computed in two
places, one of them is wrong:

- `src/lib/timing/positionsAt.ts` — `positionsAt(play, beatIndex, t, phase)`. Pure. No refs,
  no mutation, no component state. Every consumer calls it.
- `src/lib/play/validation.ts` — `validatePlay()`. Runs in the builder on every edit, in the
  importer before save, and before a play is quizzed. It implements spec rules 1–10 and 12.
  Rule 11 (derived-action ratio) is deliberately **not** here — it is a property of a whole
  playbook, not one play, and is enforced by a canary test in `tests/play/validatePlay.test.ts`.
- `src/components/court/CourtRenderer.tsx` — the only renderer.
- `src/components/animator/PlayAnimator.tsx` — the only animator.

### Coordinates

500 × 470, baseline at **top** (y=0), half court at bottom. Hoop (250, 52.5), lane x=170–330.

`src/lib/play/geometry.ts` holds court size, the off-court margin, and the movement caps
validation uses. `src/lib/court/courtLines.ts` holds the drawn-line geometry — `HOOP`, the
lane, the three-point arc — and re-exports the dimensions rather than redefining them.

The same system is mirrored in `services/importer/derive.py` and in the AI skill file, so a
change to the court must be made in all three.

### Beat semantics — the easy thing to get wrong

`Beat` carries two position maps and two ball fields:

- `startPos` / `startBall` — parser observations at the **start** of the frame.
- `pos` / `ball` — state at the **end** of the beat, i.e. the next frame's start.

`normalizeSeedPlay()` (`src/lib/play/normalize.ts`) resolves a beat's `pos` from the *next*
beat's `startPos`, which is what makes drawn arrows line up with FastDraw frames. Animation
interpolates `startPos → pos`; `ball` is derived, never hand-authored.

### Timing

The coach never sets timing. `sequenceBeat()` (`src/lib/timing/sequence.ts`) assigns each
action a default lane by classified kind (screen / cut / cut-off-screen / dribble / roll /
handoff / pass), pushes start times later per the dependency rules, then rescales so the last
action ends at 1.0. Playback is two phases per beat: MOVE (t 0→1) then a mandatory HOLD.
The ball is its own entity computed every frame — never "attached to player X" at render time.

### Builder state

`PlayBuilder` holds one `History<Play>` (`src/lib/play/history.ts`) and every edit goes
through its `mutate()`. That single path is what makes undo total — a component reaching
for `setPlayBeats` directly would produce a change undo cannot reverse.

Ops never patch a neighbouring beat. They return beats; `setPlayBeats()` restores both
chain invariants exactly once via `linkBeatPositions()` and `linkBeatBall()`. Hand-patching
`startPos` destroys the evidence of whether a later player was holding or was placed
deliberately, which is the distinction `linkBeatPositions` depends on.

Saving POSTs to `src/app/api/plays/route.ts` (Supabase; schema in `supabase/migrations/`).
The response's version number is written back with `replacePresent()` — a save echo is not
an edit and must not become an undo step.

### Import pipeline (`services/importer/`)

1. `parser.py` — deterministic, free, verified 36/36 on a real FastDraw export. **Do not
   rewrite it.** Extracts court bounds, five positions per frame, possession from the circled
   number, play names, frame order.
2. `interpret.py` — one Claude vision call per frame crop. Loads
   `docs/skills/play-interpretation.md` **at runtime** so the prompt can be tuned without a
   deploy. The model reads arrows only; it never estimates positions.
3. `derive.py` — the single writer of `beat.ball` and of movement actions the AI missed.
   Anything it invents is marked `derived: true`. Spec rule 11 caps the derived ratio across
   a playbook — checked by the seed canary test, not by `validatePlay()`.

`scripts/rebuild-seed-plays.py` and `scripts/run-interpret-v2.py` re-run this pipeline into
`src/data/`. `plays-interpreted.json` is the canonical seed everything imports, and the only
play data in `src/data/` apart from `repairs-review.json`. Re-interpretation runs (the former
`-v2` / `-v3` files) were compared against it and deleted; if you re-run the pipeline, compare
before replacing the seed rather than swapping it in. See `DECISIONS.md`.

## Conventions

- Path alias `@/*` → `src/*`. Tests live outside tsconfig's `include`, so they import with
  relative paths and explicit `.js` extensions (`../../src/lib/play/normalize.js`).
- Tests use the built-in `node:test` runner via `tsx`. No Jest/Vitest.
- The play engine (`lib/play`, `lib/timing`, `lib/court`, `components/`) is TypeScript. Auth,
  landing page, and Supabase glue are still `.jsx`/`.js` — that split is deliberate, not debt
  to clean up opportunistically.
- `_legacy/` is the previous build, excluded from tsconfig and eslint. Read it for reference;
  don't import from it. `tools/poppler/` is a gitignored local binary drop.
- Middleware lives at `src/proxy.js`, not `src/middleware.js` — this Next version renamed it,
  and the build output labels it `ƒ Proxy (Middleware)`.
- Dev harnesses: `/dev/animator` (play picker, speed, step mode, per-beat action table),
  `/dev/court` (static render check), `/dev/repairs` (crop vs. our render, for import review),
  `/plays/new` (builder).

## Rules for the whole build

- Never save, animate, assign, or quiz on a play where `valid` is false.
- No AI calls during a quiz session, ever.
- All progress data in Postgres (Supabase). localStorage only for the logged-out demo.
- AI context files live in `docs/skills/` and are loaded at runtime, never pasted into code.
- The product teaches memorization only — where to line up, where to go, who gets the ball.
  Reads, decisions, and basketball IQ are permanently out of scope. Every question must have
  one objectively correct answer checkable against the play data.

## State of the build

Phases 1–3 (foundation, builder, timing engine + animator) are in. Phase 4 (PDF import) runs
end-to-end against the real playbook; the seed in `src/data/plays-interpreted.json` came from
it and validates 12/12.

Active work is Phase 2 polish — builder drawing, undo/redo, save error states — not Phase 4.
Treat further import-pipeline work as out of scope unless asked for directly.

Phases 5–9 (review UI, teams, assignments, quiz engine, progress) are not built.
`src/lib/quiz/` is a one-line stub, and there are no `coach/` or `player/` routes: the app
routes that exist are `/`, `/login`, `/signup`, `/auth/enter`, `/plays/new`, and the three
`/dev/*` harnesses.
